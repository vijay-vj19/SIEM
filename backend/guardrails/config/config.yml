models:
  - type: main
    engine: openai
    model: gpt-4o-mini

rails:
  input:
    flows:
      - block prompt injection
      - check siem data only
  output:
    flows:
      - ensure valid verdict
